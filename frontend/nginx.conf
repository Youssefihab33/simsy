# This block is for Certbot validation via HTTP and serving React app
server {
    listen 80;
    server_name simsy.redirectme.net; # Make sure this matches domain
    server_name 192.168.1.100; # Local network IP for Internal access

    # This location block is crucial for Certbot
    location /.well-known/acme-challenge/ {
        root /var/www/certbot; # This MUST match the volume mount in docker-compose.yml
    }

    # This is the root for React app for HTTP requests.
    # It's important that this doesn't interfere with the .well-known path.
    location / {
        root /usr/share/nginx/html; # Path to React app's build directory inside container
        index index.html index.htm;
        try_files $uri $uri/ /index.html;
    }

    # Your existing API, videos, static, media locations
    location /api/ {
        proxy_pass http://backend:80/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-NginX-Proxy true;
    }

    location /videos/ {
        alias /usr/src/data/videos/;
    }

    location /static/ {
        alias /usr/src/app/simsy/staticfiles/;
        expires 30d;
        add_header Cache-Control "public";
    }

    location /media/ {
        alias /usr/src/data/media/;
        expires 7d;
        add_header Cache-Control "public";
    }
}
